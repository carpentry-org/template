(load "../main.carp")
(load "Test.carp")
(use Test)
(use MyMod)

(deftest test
  (assert-equal test
                "Hello, Carp!"
                &(greet "Carp")
                "greet returns a friendly string"
  )
)
